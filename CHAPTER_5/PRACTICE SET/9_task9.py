s={8,7,12,"Harry",[1,2]}
print(s) 
# And then we cannot change the values inside a list contained in a set in python
#  In fact, we cannot even have a list as an element in a set because sets in Python 
#  all their elements to be immutable and hashable lists are mutable and  not hashable, so they can not be added to a set
#chapter 6 3:09
